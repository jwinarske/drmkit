# SPDX-FileCopyrightText: (c) 2026 Joel Winarske
# SPDX-License-Identifier: MIT

[package]
name = "drmkit-testkit"
description = "Test-only support shared across drmkit's device suites."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false

[dependencies]
# For the DRM version ioctl: the driver's own name, which sysfs does not
# give for a device bound through the faux bus.
drm = { workspace = true }

[lints]
workspace = true
